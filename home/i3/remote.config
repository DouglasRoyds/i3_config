# ------------------------------------------- Running i3 on a local display -----------------------------------------

# Alt key when we are running i3 remotely,
# as the Windows key will most likely be commandeered by the local WM,
# whether it's Windows or Linux.
set $mod Mod1

